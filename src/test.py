from visualisation import make_map
from tobject import TObject

l = [TObject({'longitude': '47.6971825',
              'latitude': '-0.0693341',
              'object_name': 'DAVID'})]

make_map.make_map(l)
